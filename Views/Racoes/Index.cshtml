@model IEnumerable<AlmirTrabs.Models.Racoes>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Listagem de Rações</h1>

<p>
    <a class="btn btn-info" role="button" asp-controller="Racoes" asp-action="Create">Cadastrar</a>
</p>
<table class="table" id="table-Racoes">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.descricao)
            </td>
            <td>
                    <a role="button" class="btn btn-primary" asp-action="Edit" asp-route-id="@item.id">Editar</a> |
                    <a role="button" class="btn btn-warning" asp-action="Details" asp-route-id="@item.id">Detalhes</a> |
                    <a role="button" class="btn btn-danger" asp-action="Delete" asp-route-id="@item.id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
