@model IEnumerable<AlmirTrabs.Models.Pets>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Listagem de Pets</h1>

<p>
    <a class="btn btn-info" role="button" asp-controller="Pets" asp-action="Create">Cadastrar</a>
</p>
<table class="table" id="table-Pets">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.raca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.valor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.raca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.valor)
            </td>
            <td>
                    <a role="button" class="btn btn-primary" asp-action="Edit" asp-route-id="@item.id">Editar</a> |
                    <a role="button" class="btn btn-warning" asp-action="Details" asp-route-id="@item.id">Detalhes</a> |
                    <a role="button" class="btn btn-danger" asp-action="Delete" asp-route-id="@item.id">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
